# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
/build
/bin
/sshw

# Local notes and design docs (not shipped with the repo)
/docs

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Misc
/dist
.idea

!.goreleaser.yml
